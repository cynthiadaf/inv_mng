1. Select from a list of sessions (client will be in the session ID name)
2. Select the session instead of the client to resolve to the summing of values
3. Update and Delete Functionality
4. Move Login to Nav Bar
6. Add date and time and using length you can create calendar views (in front end)

8. Create another unique identifier for ease of selection of session

9. Make sure to only add sessions that have not been added to future invoices. (or atleast filter when creating in front end)


python manage.py runserver 

python manage.py makemigrations
python manage.py migrate